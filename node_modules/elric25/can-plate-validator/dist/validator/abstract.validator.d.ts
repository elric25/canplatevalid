import { Pattern } from './pattern';
export declare abstract class AbstractValidator {
    private readonly patterns;
    constructor(patterns: Pattern[]);
    static normalizeLicensePlateNumber(pattern: Pattern, licensePlateNumber: string): string;
    format(licensePlateNumber: string): string[];
    validate(licensePlateNumber: string): boolean;
}
